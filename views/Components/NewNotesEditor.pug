extends ../notes.pug
block editor 
    form(action="/notes", method="post")
        input(type="text" name="title" placeholder="Title") 
        input(type="hidden" name="author" value="Me")
        textarea#note-body(name="body", placeholder="Notes", cols="30", rows="10") 
        button(type='submit')= "Save"

